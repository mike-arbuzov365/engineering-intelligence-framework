---
type: claim_register
status: validated
scope: framework
created: 2026-07-15
review_after: 2026-10-15
related:
  - ../../examples/demo-workspace/README.md
  - ../../examples/demo-cursor-workspace/README.md
  - ../../adapters/claude-code/README.md
  - ../../adapters/cursor/README.md
  - ../../adapters/codex/README.md
  - ../../adapters/hermes/README.md
  - ../../adapters/parity-matrix.json
  - ../../adapters/switch-matrix.json
  - ../benchmarks/README.md
---

# Public claims evidence ledger

<!-- Knowledge source: vertical slice + takeover review, 2026-07-15. This
document is the source of truth for what README/website/article/LinkedIn
copy is allowed to say. A claim only moves to OBSERVED when this file names
the specific evidence for it - not on the strength of how the framework is
supposed to work.

type: claim_register (see core/ontology/knowledge-types.md#claim_register).
It was briefly mistyped as decision_register; corrected in the takeover
review because claims are not decisions - a decision register tracks what
was decided, a claim register tracks how strongly evidence supports a public
claim. It holds many independently-statused entries, so it carries no single
frontmatter `evidence` label; its own status: validated means the table
below accurately reflects each claim's evidence status, not that every claim
is OBSERVED. -->

Before writing any public-facing claim about EIF (README, website,
article, LinkedIn post, demo video, FAQ), check this table. A status of
`OBSERVED` requires the Evidence column to name something concrete and
reproducible - a command, a file, a test - not "the design supports this."

## Status definitions

- **OBSERVED** - directly exercised and verified in this repository, with
  evidence named below.
- **UNVERIFIED** - plausible, planned, or partially built, but not yet
  backed by a reproducible test or measurement.
- **NOT TESTED** - explicitly out of scope for what has been built so far
  (not a judgment that it wouldn't work - just that nothing here tests it).

## Claims

| Claim | Status | Evidence | Limitations | Allowed public wording | Forbidden wording |
|---|---|---|---|---|---|
| EIF runs end to end | OBSERVED | `scripts/tests/test_journey.py` (273 checks) - a real subprocess journey against a fresh seed project: init, config/lock validation, runtime materialization, English+Ukrainian retrieval, task-scope, failing-before/passing-after test, config-driven Ukrainian Knowledge Delta + closeout written to real files, artifact/privacy/link validation from the instance's own bundle, a routine upgrade actually driven by the existing config (not CLI defaults), explicit `--force` reconfiguration, a migration status surviving a flagless upgrade, the generated retrieval command's `--framework-root`, strict-vs-`--draft` rendering, `eif_verify_runtime.py` against clean/corrupted instances, a malformed-marker refusal, a second independent instance, and injected failures at all five real transaction-commit boundaries (runtime/lock/entrypoint/`.gitignore`/knowledge index when EIF-managed) with proven rollback - plus mid-copy partial-write cleanup at every staging boundary (a genuinely half-written `.next` is still removed) and repository-origin provenance (an existing repo with no `CLAUDE.md` records `adopted`, not `greenfield`; an unreadable origin fails closed) | One scenario (a leap-year calculator), one adapter (Claude Code), no multi-repo, no Graphify/RTK, single operator this session, not yet reproduced by an independent human | "The v0.1 vertical slice runs end to end - a real subprocess-driven journey, including an injected-failure rollback proof at every commit stage, every pre-commit staging boundary, and every mid-copy partial write - against one synthetic demo project instance." | "EIF works." "Production-ready." "Proven at scale." |
| The quickstart's mechanical execution time is known | OBSERVED (partial) | `docs/guides/quickstart.md`'s command sequence (init, retrieval, the demo's test, both renders, all five validation checks, plus staging the generated files so the privacy scan's `git ls-files`-based check sees them) was run as a scripted, back-to-back sequence and timed with `Measure-Command` three separate times: 6.3s, 6.4s, 7.0s (average ~6.6s). An earlier single-run measurement (5.7s) omitted the staging step and undercounted; superseded by this one, not averaged into it, since it measured a genuinely different (incomplete) sequence | This is TOOL execution time only - the human reading/typing/comprehension time a "10-minute quickstart" claim is actually about remains untimed by an independent human; measured on one machine, one run count (3), not a statistically rigorous benchmark; the sequence does not include installing Python or cloning the repository | "Mechanical execution of the quickstart's full command sequence was measured at 6.3-7.0 seconds across three runs on one machine - tool execution time, not a claim about how long a human takes to read and follow the guide." | "10-minute quickstart, verified." "Fast setup." (implying the human-facing time, which is not what was measured) |
| EIF can initialize a separate, runnable, upgradeable project instance | OBSERVED | `scripts/eif_init.py` initialized an instance in a temp directory *outside* the framework checkout with real dirty-checked provenance and a sha256-hashed bundle manifest; the pinned `.eif/runtime/` source bundle then ran standalone (search, validate, render, self-verify) with no framework on the path. Mode (init / routine upgrade / explicit `--force` reconfigure) is decided by what's on disk: a routine upgrade derives project/adapter/locale/**migration status** from the existing config and lock - proven by setting `migration_status: adopted`, then re-running with zero flags and confirming it survives (the prior behavior silently reset it to `greenfield`) - and leaves `.eif/config.yaml` byte-for-byte unchanged; `--force` is the only path that changes it, and only the fields actually passed. Every managed artifact - config (when written), runtime, lock, entrypoint, `.gitignore`, and the knowledge index (when `knowledge.managed: true`) - now commits as **one ordered transaction**: injecting a failure after each commit stage, before each pre-commit staging write, AND partway through each staging write/copy (leaving a genuinely partial `.next` on disk), in separate runs, and confirming in every case that the tree returns byte-for-byte to its prior state - every already-committed stage rolled back, no `.next`/`.previous` files, no orphaned new directories (including a fresh-init `.eif/`), and no leftover `--force` config backup on a failed reconfigure (a backup is kept only on a SUCCESSFUL reconfigure, as the documented recovery artifact) - with the instance passing both `eif_verify_runtime.py` and a real retrieval command afterward - not just a non-zero exit code. `knowledge.root`/`knowledge.index_path` are validated by a shared path-policy helper that rejects absolute paths, Windows drive/UNC paths, `..` traversal, and any value resolving outside the instance. A CLAUDE.md/`.gitignore` with malformed (missing-partner/reversed/duplicated) `BEGIN`/`END` markers is refused before anything is written, not silently merged into corrupted output. An existing but broken `.eif/config.yaml` (invalid YAML, not a mapping, or schema-invalid) stops before any write rather than being treated as absent and silently re-initialized. `scripts/tests/test_init.py` (72 checks), `test_markers.py` (12 checks), `test_paths.py` (32 checks) + `test_journey.py` | Experimental bootstrap, not a stable CLI - does not ratify D-05/D-08; the bundle is a pinned **source** copy, not a self-contained interpreter environment (`pip install` is still required once); the injected-failure mechanism now proves rollback both *between* commit stages and *during* an individual write/copy (a genuinely partial `.next` left on disk, then cleaned up) - it does not simulate a crash *during* an individual atomic rename, which the OS already guarantees; a marker-conflict refusal is a **preflight** refusal (nothing staged or written), a distinct guarantee from mid-transaction rollback, not the same evidence; only exercised on Windows this session | "An experimental bootstrap initializes a project instance with real, dirty-checked framework provenance and a hashed runtime source bundle. A routine upgrade is driven by the existing config, not CLI flags, and preserves settings like migration status. Every managed file commits as one transaction with a proven rollback path - to the exact prior state, with no orphaned files - if any stage fails partway, and a malformed entrypoint is refused rather than corrupted." | "Official CLI." "Zero-copy." "Fully self-contained." "Crash-safe at any point." (an installable `eifctl` package now exists, and D-05/D-08 are Ratified - see the row below - but this experimental-bootstrap claim itself is about the older, separate `eif_init.py` invocation, not the packaged command) |
| An installable `eifctl` package works without a framework git checkout, including a full synthetic-task journey | OBSERVED, D-05/D-08 Ratified | A real wheel built from `pyproject.toml`, installed into a clean venv whose own path contains a space and non-ASCII text, gives a working `eifctl` console command with 7 subcommands (`init`, `doctor`, `search`, `render`, `privacy-scan`, `validate`, `version`) - each a thin wrapper over the exact same `scripts/eif_*.py` functions used standalone (byte-for-byte synced, `scripts/sync_package_sources.py`), never a fork. Beyond checking each subcommand in isolation, a full synthetic-task journey - new task (pre-existing project files, no entrypoint yet) -> `eifctl init` bootstraps EIF onto it -> `eifctl search` retrieves the seeded fact and failure pattern WITH the authority metadata (`status=validated`, `evidence=OBSERVED`, `confidence=high`) a retrieval-before-implementation step actually relies on to weigh a source -> the pre-authored implementation passes its real test -> `eifctl render knowledge-delta`/`session-closeout` (the promotion-decision and cleanup steps) write real files in the instance's configured locale (Ukrainian, not a silent English fallback) -> `eifctl validate`/`privacy-scan`/`doctor` all pass - reusing the same fixture `examples/demo-workspace/README.md` documents, not a second, drifting copy of it. `eifctl init --adapter codex`/`--adapter hermes` also proven from the wheel (not just `claude-code`) - the wheel's bundled adapter registry and Hermes's resolver/simulator round-trip through packaging, not just the dev checkout. The expanded `scripts/tests/test_package_build.py` passes 60/60 locally, including disabled/unavailable/degraded/healthy machine-readable integration states, the standalone benchmark C/D contract tool and injected upgrade rollback; its preceding 49-check form ran on the Windows + Ubuntu x Python 3.11/3.12 matrix, while the expanded matrix remains a manual release gate | Not published to PyPI; the journey uses `claude-code`'s adapter for its own bootstrap step (Codex/Hermes proven only for `init`+`doctor`, not the full journey) and does not exercise a genuine multi-source authority CONFLICT (the seeded fact and failure pattern agree, so "authority resolution" here means the metadata an agent uses to trust a source, not adjudicating between disagreeing ones); the enabled integration status matrix uses deterministic local test doubles to prove package/doctor transport, while real provider behavior is bounded by the separate Graphify/RTK rows below; benchmark C/D packaging proves delivery of the contract, not a real-agent C/D result | "A real, installable `eifctl` package exists with 7 subcommands, verified end to end - including a full synthetic-task journey (retrieval, implementation, tests, Knowledge Delta, promotion decision, cleanup) from the installed wheel. The expanded 60-check suite passes locally; the updated cross-platform matrix remains a release gate." | "Published package." "Official release." "Proves authority-conflict resolution." "Graphify/RTK real-provider behavior is verified by package test doubles." |
| EIF can be adopted onto an existing, already-governed repository without silently overriding it | OBSERVED | Adoption-hardening round, direct response to real findings from a throwaway-copy pilot against a private project-layer repository (see the private planning packet - not reproduced here, no private content). `.eif/config.yaml` gained `knowledge.root`/`knowledge.index_path` (generated instructions, `eif_generate_index.py`, and `eif_search_knowledge.py` invocations all read the configured path, not a hardcoded `knowledge/`) and `adoption.mode` (`greenfield`\|`coexist`, user-owned, deliberately NOT in `framework.lock.yaml`). A new adoption preflight (`scripts/eif_preflight.py`) runs before any write: if the entrypoint file already has substantial content and no `--adoption-mode` was given, `eif_init.py` **stops before writing anything** rather than appending a second, competing authority statement - `--dry-run` shows the same OK/WARN/STOP report a real run enforces. In `coexist` mode the generated block states it is NOT the project's sole/primary authority and defers to pre-existing rules. `scripts/eif_privacy_scan.py` gained a narrow, finding-specific suppression mechanism (`privacy.suppressions` in config: rule + exact repo-relative path + a content fingerprint identifying one specific finding + rationale + review date + optional expiry - never a whole rule+file, never a glob; never printed matched values; an expired, no-longer-matching, or malformed suppression is itself reported as a finding, not silently kept or accepted). An independent-review follow-up closed three further gaps: a shared path-policy helper (`scripts/eif_paths.py`) rejects `knowledge.root`/`knowledge.index_path` values that are absolute, a Windows drive/UNC path, contain `..`, or resolve outside the instance; the preflight now keys its STOP/WARN decision off the resolved, *persisted* adoption basis (not just the current run's CLI flags): a run with no adoption decision on record STOPs uniformly regardless of mode (init/upgrade/reconfigure), while a persisted `coexist` decision authorizes safe continuation on a later flagless run (it prevents a fall-back to implicit greenfield - it does not itself STOP); an existing knowledge-index file without EIF's own generated-file marker is never overwritten, with the index write folded into the same transaction as every other managed artifact; a strict path-policy allowlist rejects shell metacharacters (quotes, backticks, `$`, `%`, `;`, `&`, `|`, `<`, `>`, `()`, control chars) so a configured path is always safe to quote into the generated commands (which now quote it); and `lock.instance.migration_status` is derived from a dedicated read-only **repository-origin** check (`eif_init.detect_repository_origin`) - separate from the governance/entrypoint preflight - so an existing code repository with a README or `src/` tree but **no `CLAUDE.md`** is correctly recorded `adopted`, not `greenfield` (the earlier entrypoint-only derivation got exactly this case wrong); an unreadable origin fails closed (requires an explicit `--migration-status`) rather than guessing `greenfield`; coexist implies an adopted history; and a greenfield authority override on a pre-existing repo keeps the adopted history. Separately, `scripts/eif_privacy_scan.py` now fails loudly (exit 1) on a `.eif/config.yaml` that exists but is empty, invalid YAML, not a mapping, or unreadable, instead of treating it as a valid suppression-free config. `scripts/tests/test_adoption.py` (67 checks) proves, against a realistic sanitized fixture (large pre-existing `CLAUDE.md` with its own governance section, nonstandard `docs/knowledge/` path, a TypeScript OS-keychain interface that pattern-matches the privacy scanner's `password_assignment` rule the same way a real one did during the pilot, and a genuinely secret-shaped fixture value): dry-run writes zero files; an unconfigured coexistence conflict stops before any write; a configured `coexist` run writes to the configured paths, not the greenfield defaults, with no root `knowledge/` silently created; every line of the pre-existing file is preserved unmodified; the keychain false positive is suppressed narrowly while the unrelated real secret-shaped fixture still fails the scan; link check and runtime verification pass after install and after a routine upgrade; and rollback (`.eif/` deleted, tracked files `git checkout`'d) restores the tree exactly - a real gap this same test run found and fixed: the documented rollback did not by itself account for a generated knowledge-index file living outside `.eif/` | Preflight's "existing governance" detection is a content-presence heuristic (a length threshold on the existing entrypoint file), not semantic - it can both over-trigger (a long non-governance file) and under-trigger (terse real governance below the threshold); one fixture PLUS the full pilot re-run against fresh disposable copies of one real private pilot target each round - the live repository is never opened for writing, and a second, different real target is still not exercised; a suppression's fingerprint is computed from the rule, path, and *stripped line text* - it survives the matched line moving to a different line number in the file, but a suppression stops matching (and reactivates as a finding) the moment the line's text itself changes, even trivially | "An adoption preflight detects pre-existing project governance before any write and refuses to proceed without an explicit coexistence decision; in coexist mode the generated block defers to existing rules instead of declaring itself sole authority; knowledge paths and privacy-scan suppressions are user-configured, not hardcoded." | "Automatically merges with any existing setup." "Zero-config adoption." "Understands your governance." |
| A project instance can audit its own integrity | OBSERVED | `scripts/eif_verify_runtime.py` - bundled into every instance, checks config/lock schema validity, the lock's manifest digest against a fresh recompute, every manifested bundle file's sha256 against its current on-disk content, missing-vs-unexpected files (`README.md`/`__pycache__` excluded by design - anything else unlisted is a real finding, not silently ignored), config/adapter-registry/lock/entrypoint consistency, provenance (dirty/asserted) notes, marker integrity, and (independent-review addition) drift between `.eif/config.yaml` and the generated entrypoint block (`adoption.mode` vs. the authority-section text, `knowledge.root`/`knowledge.index_path` vs. the generated commands) or the knowledge index (missing file, stripped ownership marker, or hash mismatch against what the lock recorded) - a hand-edit with no regeneration fails with a specific "run eif_init.py again" instruction, not a silent "all checks passed", in one pass, with no framework checkout present. `scripts/tests/test_verify_runtime.py` (37 checks, built against a real generated instance then corrupted one way at a time) + `test_journey.py` steps 24-26, 29 | Checks the bundle's manifest scope as designed (see the Limitations column above) - it is not a general-purpose file-integrity tool and does not scan outside `.eif/`, `CLAUDE.md`, and `.gitignore` | "A project instance can audit its own config/lock schema validity, bundle file integrity, adapter consistency, entrypoint marker integrity, and drift between its config and its generated entrypoint block or knowledge index, from its own bundle, with a precisely scoped, documented notion of what counts as an integrity finding." | "Detects any tampering." "Full integrity guarantee." |
| EIF retrieves relevant prior engineering knowledge (lifecycle-aware, Unicode, schema-aware) | OBSERVED | `eif_search_knowledge.py "leap year"` returned the relevant failure pattern first and changed the implementation actually written; a Ukrainian query (`високосний`) matched Ukrainian content; a `rejected` hypothesis was excluded by default and reported as status-ineligible; an unparseable-YAML artifact and a schema-invalid artifact (parses fine, violates the ontology) are reported in two *distinct* categories, neither confused with "no results." The generated CLAUDE.md's own search command includes `--framework-root` by default (a prior version omitted it, so a freshly-generated instance's first retrieval silently ran schema-unaware) - asserted directly against the generated file's text, not just the template source, in `test_journey.py` step 21. (`scripts/tests/test_search_knowledge.py`, 11 checks; `test_generate_index.py`, 12 checks) | Offline keyword/substring scoring only, no semantic search; tested against small seeded indexes, not a realistic-sized knowledge base; schema-invalid detection still requires `--framework-root` to be passed to whatever *invokes* the search - the fix only guarantees the command EIF itself generates includes it, not a hand-typed one | "Offline, lifecycle-aware, schema-aware keyword retrieval surfaces relevant validated knowledge (including Ukrainian), excludes rejected/superseded by default, reports unparseable vs. schema-invalid vs. not-found as three distinct, honest outcomes, and the generated retrieval command is schema-aware by default." | "Semantic search." "AI-powered retrieval." "Understands your codebase." |
| EIF supports Ukrainian project-facing documentation and retrieval | OBSERVED (partial) | `locales/uk/` rendered for real via the committed, config-driven `scripts/eif_render.py` command - run with **no `--locale` flag**, it reads `documentation_locale` from `.eif/config.yaml` and writes real `knowledge-delta.md`/`session-closeout.md` files (not stdout-only); real Ukrainian status messages from `eif_init.py`; Ukrainian knowledge retrieval verified; English fallback verified for both missing-config and unknown-locale cases. Rendering is now truthful about failure: a config that EXISTS but is broken (bad YAML, not a mapping, or missing `documentation_locale`) fails loudly instead of silently rendering in English as if the config were simply absent - a missing config and a broken one are deliberately not the same case. A "final" render (the default) with any unresolved `{placeholder}` token now fails; `--draft` explicitly opts into a partial render instead of that being the silent, unlabeled default. Writes are atomic (temp file + rename). (`scripts/tests/test_render.py`, 22 checks) | 4 surfaces (status messages, Knowledge Delta, closeout headings, retrieval) - not full agent-response localization; `terminology.yaml` not populated; D-06/D-07 remain open, not ratified | "The slice generates Ukrainian Knowledge Delta, closeout headings and status messages through a config-driven locale layer that writes real files atomically, fails loudly (not silently in English) on a broken config, requires an explicit `--draft` for a partial render, retrieves Ukrainian knowledge, and falls back to English only when there is genuinely no config." | "Fully localized." "Native Ukrainian support." "Multi-language" (implying more than the 2 locales that exist). |
| The Claude Code adapter's entrypoint is generated correctly | OBSERVED (partial) | Claude Code CLI `2.1.169` verified live 2026-07-15; official docs confirm Claude Code loads `CLAUDE.md` at session start (Context7, code.claude.com/docs/en/memory), so `eif_init` generates `CLAUDE.md`, not AGENTS.md, via a restricted adapter registry (`--adapter` only accepts registered names - an unsupported adapter name is rejected, not silently mapped to the wrong entrypoint); instruction/skill discovery OBSERVED in-session | Hook rewrite behavior NOT re-verified end-to-end this round - carried over from dated (2026-06-16) private evidence, flagged in `adapters/claude-code/README.md` | "The bootstrap generates the correct Claude Code entrypoint (CLAUDE.md, the file it actually loads) from a restricted adapter registry; instruction and skill discovery were verified live; hook behavior is carried over, not re-verified this round." | "Fully verified adapter." "Hooks guaranteed to work." "Any adapter name works." |
| The Cursor adapter's entrypoint is generated correctly (code/test observed) | OBSERVED | Cursor's current, official Rules format (`.cursor/rules/*.mdc`, frontmatter `description`/`globs`/`alwaysApply`) was fetched live from `cursor.com/docs` 2026-07-16 (browser fetch + a DOM substring check, not from training-data memory - a web-search-surfaced claim that Cursor also reads CLAUDE.md was checked against the same fetched page and found unsupported, so it is not relied on anywhere in this adapter). `eif_init` generates a dedicated, exclusively EIF-owned `.cursor/rules/eif/governance.mdc` (never a shared file) via the same restricted adapter registry. `scripts/tests/test_cursor_adapter.py` (74 checks): greenfield init; nested-entrypoint transaction rollback at multiple fault points including a pre-existing sibling directory surviving untouched; flagless upgrade; bidirectional adapter switching (old entrypoint stripped or deleted correctly, malformed old markers STOP before any write, fault-injected rollback after the old-entrypoint commit restores it byte-for-byte, exactly one active EIF block after a successful switch either direction); adapter-aware governance discovery (a sibling `.cursor/rules/*.mdc` file, legacy `.cursorrules`, or `AGENTS.md` with no adoption decision STOPs, coexist preserves them untouched); the exact EIF target with foreign unmarked content STOPs under both coexist and an explicit greenfield override (a real bug found and fixed: an earlier version unconditionally overwrote unproven content at this path); malformed EIF target on a plain upgrade STOPs; EN/UK generation; the generated search command is extracted and actually executed against a seeded fixture, not just checked for text presence; privacy scan and doctor pass on a Cursor instance. `adapters/parity-matrix.json` + `scripts/tests/test_parity_matrix.py` (8 checks) - a drift test that fails if a registered adapter or dimension is added without updating the matrix | Hook-based enforcement not built for this adapter (matches Claude Code - neither ships hooks); coexist/AGENTS.md-detection is generic governance-discovery logic exercised against synthetic fixtures, not a real pre-existing Cursor project | "The Cursor adapter's dedicated entrypoint (`.cursor/rules/eif/governance.mdc`) is generated correctly against the current official Rules format, with 74 executable acceptance checks covering nested-directory transactions, adapter switching, governance discovery, and fail-closed ownership checks." | "Fully runtime validated." "Two production-ready adapters." "Cursor always loads EIF rules." |
| The Cursor adapter's generated file is actually consumed by Cursor at runtime | UNVERIFIED | Cursor 3.11.19 is installed locally; launching it via computer-use against a synthetic `eif_init.py --adapter cursor` project confirmed Cursor opens the project and its file Explorer correctly shows the generated files (`.cursor/`, `.eif/`, `.gitignore`) - real, but partial: computer-use access to IDE/terminal applications is click-tier only (no typing, unreliable screenshot content), so no Agent-chat response has been observed reflecting the generated rule's content. See `examples/demo-cursor-workspace/MANUAL-RUNTIME-CHECK.md` for the exact deterministic, unguessable-probe procedure a human needs to run to close this | The one remaining manual gate for this adapter - not yet performed by anyone as of this writing | "The generated file opens correctly in Cursor and appears in its file Explorer; whether Cursor's Agent chat includes its content in a response has not yet been manually confirmed." | "Runtime validated." "Cursor always loads EIF rules." "Confirmed working in Cursor." |
| The Codex adapter's active entrypoint is resolved and generated correctly | OBSERVED | Verified against Codex's own primary Rust source (`codex-rs/core/src/agents_md.rs`, `find_nearest_ancestor_with_markers`, `config.schema.json`), pinned at commit `3a067484584861606ad842de5bc4ac735a865ddf` - not a secondary documentation page. `eif_init` resolves and marker-merges into whichever of `AGENTS.override.md`/`AGENTS.md`/a configured `project_doc_fallback_filenames` entry is actually active (`entry_strategy=dynamic-resolve`); a same-directory `AGENTS.override.md` correctly makes a sibling `AGENTS.md` a dead file EIF never merges into. Real runtime proof via `codex debug prompt-input` (no auth required) from an isolated, freshly-created empty `CODEX_HOME`: a marker-less ancestry produced zero occurrences of a 40000-byte decoy file's unguessable marker in the captured prompt input (proving no ancestor content is read once the walk exhausts); a `project_doc_max_bytes` limit below the real file size produced a captured prompt input containing the exact predicted byte-for-byte truncation prefix, with the next byte confirmed absent (ruling out an off-by-one match). `scripts/tests/test_codex_adapter.py`: 123/123. Two real P1 defects (project-root discovery walking to the filesystem root with no marker anywhere; the size budget modeled as whole-file skip instead of Codex's real mid-file truncation) were found in an independent post-merge review - traced to the original implementation being verified only against a secondary docs page - and fixed in a hotfix round; see `adapters/codex/README.md` and the corresponding private evidence packet | Codex Cloud's own hosted environment and the ChatGPT desktop app's `/init` flow are not independently runtime-verified (only the CLI's `debug prompt-input` command was used); `hooks.json` PreToolUse hook rewrite support is a different, unported mechanism, not covered by this claim; `adapter.options.codex.*` (including `project_root_markers`) is this instance's own asserted configuration - no authless command exposes Codex's real, live `config.toml` state, so the two must be kept in sync by hand; Codex remains an experimental-supported adapter (D-09), not one of the two required v0.1 adapters | "The Codex adapter's active-entrypoint resolution was re-derived from Codex's own primary source (not documentation) and confirmed against the real installed CLI from an isolated, unauthenticated CODEX\_HOME, including a byte-exact truncation-boundary match." | "Fully verified adapter." "Production-ready." "Required v0.1 adapter." "Hooks guaranteed to work." |
| The Hermes adapter's active entrypoint is resolved and generated correctly | OBSERVED | Verified against Hermes's own installed Python source (a git install, not a black-box wheel) - the real four-tier startup discovery contract (tier 1: `.hermes.md`/`HERMES.md`, unbounded ancestor walk, cwd-only when no git root; tier 2: `AGENTS.md`/`agents.md`, cwd-only; tier 3: `CLAUDE.md`/`claude.md`, cwd-only; tier 4: `.cursorrules` + immediate `.cursor/rules/*.mdc`, cwd-only, concatenated) and the real tier-specific transformation/truncation pipeline (head/tail-preserving, character-counted, dynamic cap scaled to the model's context window unless an explicit limit is configured). `eif_init` resolves and marker-merges into whichever tier-1/2/3 file is actually active, or STOPs if the real active source is a parent directory's file or the Cursor-format tier-4 fallback. Real runtime proof via `hermes prompt-size --json` (no auth required) from an isolated, freshly-created empty `HERMES_HOME`: confirmed the real CLI's ancestor walk has no depth cap (7 levels deep, still finds the ancestor file) and that this adapter's own truncation simulator matches the real CLI's output exactly (4104-character exact-match evidence). `scripts/tests/test_hermes_adapter.py`: 77/77. Supersedes a closed, unmerged prior attempt whose own "verified" evidence (a fabricated 5-level ancestor-walk cap, missing lowercase-filename fallbacks, an unmodeled Cursor-format tier) was independently re-derived and found wrong, not reused - see `adapters/hermes/README.md`'s "What PR #12 got wrong" section. A real, pre-existing defect in the generic (adapter-agnostic) switching transaction code - a same-path collision when the old and new entrypoint coincide (e.g. `CLAUDE.md` for a Claude Code <-> Hermes switch) - was found and fixed as part of this work, not specific to Hermes | Lowercase-filename tier-2/3 precedence is platform-conditional and genuinely exercised only on case-sensitive filesystems (Ubuntu CI), not simulated on Windows; terminal-tool guard hooks are a different, unported, block-only mechanism; `adapter.options.hermes.*` is this instance's own asserted configuration, not introspected live; Hermes remains an experimental-supported adapter (D-09), not one of the two required v0.1 adapters | "The Hermes adapter's active-entrypoint resolution and truncation model were re-derived from Hermes's own installed source (not documentation) and confirmed against the real installed CLI from an isolated, unauthenticated HERMES\_HOME, including an exact truncation-output match." | "Fully verified adapter." "Production-ready." "Required v0.1 adapter." "5-level ancestor cap." (a claim from the superseded prior attempt, contradicted by the real, uncapped-walk evidence above) |
| Switching a project between any two of the four registered adapters is safe and complete | OBSERVED | A full directed switching matrix (`adapters/switch-matrix.json`) covers all 12 ordered pairs across the four registered adapters (Claude Code, Cursor, Codex, Hermes) - A->B and B->A are proven separately since they are different questions (which file is created vs. stripped/deleted, whether the destination's own governance-discovery rules require an explicit adoption decision). `scripts/tests/test_adapter_switch_matrix.py`: 81/81 - every pair proves exactly one effective EIF block after the switch, the correct active destination source, project-content preservation, exclusive-old-file removal only when EIF-owned, config/lock agreement, and doctor success, plus two representative deep-dive proofs (malformed-marker STOP, fault-injected rollback restoring the tree byte-for-byte) on the two pairs (Cursor<->Hermes, Codex<->Hermes) no adapter-specific test file covered before this round. A real cross-platform test-authoring bug (Windows case-insensitive-but-case-preserving filesystem double-counting `AGENTS.md`/`agents.md` as two separate active blocks when only one physically existed) was found and fixed while building this matrix's own test - documented as a bug in the new test, not the product | `destination_budget_survival`, `malformed_marker_stop`, and `rollback` are proven adapter-generically (the same shared transaction/marker-merge code regardless of which two adapters are involved) plus the two representative deep-dives above, not independently re-proven for all 12 pairs; no directed pair exercises a non-default `adapter.options.*` configuration active at the moment of the switch itself - see `switch-matrix.json`'s own `known_limitations` | "A full directed switching matrix across all four registered adapters (12 ordered pairs) proves exactly one active EIF block, correct destination-source resolution, project-content preservation, and doctor success after every switch, with representative fault-injection and malformed-marker proofs on the two previously-untested pairs." | "Every property independently proven for every pair." "All adapter-option combinations tested." |
| Dependency license checking is reproducible regardless of the invoking environment | OBSERVED | The prior default scanned every package `importlib.metadata` reported installed in the CURRENT interpreter - reproducible only by the accident that CI always installs exactly `requirements.txt`. Reproduced the failure mode directly on a real (non-CI) development machine: the ambient-scan mode found 137 installed packages, 4 falsely DENIED and 6 falsely UNKNOWN, none of them real EIF dependencies. Default mode now reads the committed CycloneDX `sbom.cdx.json` instead - a single deterministic file - and fails if the SBOM's pinned-component versions disagree with `requirements.txt` (a stale SBOM); the old ambient scan survives only as an explicit `--environment` opt-in. `scripts/tests/test_check_licenses.py`: 30/30, including a real (non-fixture) proof against this repository's own committed SBOM. CI's `license-check` job runs the identical suite and gate check on both `ubuntu-latest` and `windows-latest`, both returning the identical 19/19-OK, 0-denied, 0-unknown, 0-stale result - genuine cross-platform determinism evidence, not assumed | `--environment` mode itself remains, by design, only trustworthy in a clean virtualenv; SBOM regeneration after a dependency bump is still a manual step (`check_sbom_freshness` catches drift after the fact, nothing regenerates automatically); does not re-open the underlying license-family policy decisions themselves (`core/policies/license-policy.json`), only the reproducibility of how they're checked | "Dependency license checking defaults to a committed, deterministic SBOM rather than a live scan of the invoking environment, confirmed to produce identical results on Windows and Ubuntu CI." | "Every environment guaranteed clean." "Ambient scanning is deterministic." |
| A project instance can validate itself from its own bundle | OBSERVED | `.eif/runtime/` bundles schema-aware frontmatter/config/lock validation, privacy scanning, link checking, and localized rendering - all six ran as real subprocesses from an isolated instance's own bundle in `test_journey.py` steps 2, 5, 6, 11-14, with zero privacy findings and all links resolving. `docs/architecture/instance-contract.md#validation-surface` names exactly what is and is not bundled | PR-workflow-specific tooling (Knowledge Delta CI check, merge-gate script) is deliberately framework-maintainer-only, not bundled - "validate the instance" does not mean "re-implement this repository's entire CI" | "A project instance can run schema validation, privacy scanning, link checking, and localized rendering from its own pinned bundle, with no framework checkout present." | "Full CI in every instance." "Instance and framework CI are identical." |
| CI validates EIF artifacts, at a routine and a release cadence | OBSERVED | D-14 (2026-07-19) superseded the prior single-tier topology this row used to describe. `.github/workflows/ci.yml` now runs exactly ONE routine PR job (`PR smoke checks`): the critical-path `scripts/tests/smoke.py` suite (48/48 local checks: core schema/privacy sanity, all four adapters' greenfield init/doctor/idempotency/content-survival and optional-integration pointer discovery, one cross-adapter switch cycle) plus privacy scanning, frontmatter/config/lock schema validation, link checking, a YAML/JSON-Schema self-consistency check, and Knowledge Delta completeness (live PR body via `gh api`). Push to `main` triggers no workflow at all - a merged PR was already fully validated. The full 28-suite inventory (`scripts/tests/run_all.py --json`) and cross-platform package-build/license-check matrices run from `.github/workflows/release-check.yml` (`workflow_dispatch`-only) or locally. Exact check totals are run evidence, not a stable contract, because targeted regression suites evolve | Branch protection / required status checks are not configured at the GitHub settings level - CI passing is "required by policy," not technically enforced (see `docs/architecture/merge-enforcement.md`); the expanded smoke/inventory counts are local until the final hosted gate | "Routine PR validation is a single hosted job (critical-path smoke suite plus policy/privacy/link/schema/Knowledge-Delta checks); push to main does not duplicate it. The full 28-suite inventory and cross-platform package/license matrices run on a manual release gate or locally, not automatically on every PR or push." | "CI blocks merging." "Fully tested on every push." "All checks run on every PR." |
| EIF saves tokens | UNVERIFIED (first bounded pilot exists, not sufficient for a general claim) | One real-agent pilot exists (2026-07-20, D-010): `deepseek-v4-flash`, 3 fixtures (T02/T07/T10), modes A and B, one attempt each, 6 attempts total, 100% task success in both modes. Real, provider-reported token counts per `docs/benchmarks/README.md` - not estimated. n=1 per (task, mode) cell: not a statistically powered comparison, and `eif_benchmark.py aggregate` correctly reports `input_tokens_95ci: null` for every group (no confidence interval is fabricated from a single sample). Modes C/D now have deterministic integrity-contract tests that require source-bound Graphify consumption and, for D, exact-attempt RTK telemetry; their fake-runner token figures are suppressed | One model, three of ten fixtures and one attempt per A/B cell - no repeated trials or other providers/models. Modes C/D have no real-agent result. Cannot support a directional claim ("governance uses more/fewer tokens") without repeated trials | "A first bounded real-agent A/B pilot (one model, three fixtures, six total attempts) completed with 100% task success in both modes; see `docs/benchmarks/README.md` for the per-attempt token counts. Modes C/D have executable integrity contracts, but no real-agent performance result. This is not a statistically powered efficiency comparison." | Any specific "N% savings" or "tokens saved" claim. "Benchmark proves EIF is more efficient." Any claim generalized beyond these three fixtures/one model/one attempt each. |
| EIF defines bounded iterative execution | OBSERVED (methodology contract only) | `playbooks/bounded-evidence-loop.md`, `templates/bounded-evidence-loop.md`, and `skills/run-bounded-evidence-loop/SKILL.md` require observable success evidence, an evaluator, iteration and remote-run budgets, failure signatures, adaptation, and `PASS`/`BLOCKED`/`DEFERRED` closeout; `test_operating_layer.py` checks the cross-layer contract and session-layer ephemerality | This proves the workflow artifacts are internally present and structurally connected. It does not prove fewer retries, higher quality, lower cost, or reliable autonomous completion on real projects | "EIF includes a bounded evidence-loop contract for iterative agent work, with explicit evaluators, budgets, evidence, and stop conditions." | "Loops improve quality." "Autonomous until complete." "Self-correcting without limits." |
| EIF defines a three-layer engineering-intelligence model | OBSERVED (methodology contract only) | `docs/architecture/HOW-EIF-WORKS.md#the-three-layer-context-model`, `playbooks/engineering-intelligence-operating-protocol.md`, and `scripts/tests/test_operating_layer.py` define and structurally connect framework-layer reusable methodology, project-layer durable project knowledge, and ephemeral session-layer context; the test verifies that session-layer checkpoints are gitignored and that only validated learning moves through Knowledge Delta | This proves the model and workflow artifacts are present and structurally connected. It does not prove that an agent never forgets, always retrieves the right lesson, or improves task outcomes | "EIF separates reusable framework methodology, durable project knowledge and ephemeral session context into three layers, with Knowledge Delta governing promotion between them." | "Agents never forget." "Persistent memory guaranteed." "Self-learning model." |
| EIF connects experience retrieval to the session lifecycle | OBSERVED | `docs/architecture/HOW-EIF-WORKS.md#experience-retrieval`, `playbooks/session-preparation.md`, `playbooks/session-execution.md`, and `playbooks/session-closeout.md` define retrieval, scope, execution, verification and closeout; `scripts/tests/test_search_knowledge.py`, `scripts/tests/test_operating_layer.py`, and the synthetic `scripts/tests/test_journey.py` exercise the retrieval and closeout surfaces | The workflow contract is structurally tested and one synthetic journey exercises retrieval and closeout. It does not prove perfect recall or prevention of every repeated mistake | "EIF requires relevant prior-knowledge retrieval before non-trivial work and defines preparation, controlled execution, verification and closeout as the session lifecycle." | "Always retrieves the right answer." "Prevents repeated mistakes." "Perfect engineering memory." |
| EIF defines governed learning and reusable skills | OBSERVED (methodology contract only) | `docs/architecture/HOW-EIF-WORKS.md#knowledge-delta`, `docs/architecture/HOW-EIF-WORKS.md#skills-and-agent-adapters`, `playbooks/session-closeout.md`, `playbooks/knowledge-ingest.md`, and `skills/README.md` define how a validated session delta is classified, retained locally or promoted into versioned facts, rules, playbooks, templates and invokable skills; `scripts/tests/test_operating_layer.py` checks the Knowledge Delta and session-layer promotion boundary | This is externalized system-level learning through versioned artifacts. It is not model training, fine-tuning, autonomous self-modification or evidence that task outcomes improve over time | "EIF defines a governed learning flow in which a session's validated Knowledge Delta can be classified, linted, retained locally or promoted into reusable artifacts such as rules, playbooks, templates and skills." | "Self-learning AI." "Agents improve automatically." "The model learns from every session." |
| EIF can classify Graphify structural health and artifact lifecycle | OBSERVED | `scripts/tests/test_graphify_integration.py` 28/28 plus one installed-host canary on Graphify 0.9.12 (29/29); bounded three-node query/path/explain fixture; validated D-08 metadata binding source commit, Graphify version, explicit repository identity, manifest/scope hashes and graph digest; `fresh`/`code-update-required`/`semantic-update-required`/`full-rebuild-required`/`blocked`/`suppressed` lifecycle; size-bounded restore with paired graph/metadata rollback; semantic-gate tests | One local Graphify version and a tiny synthetic fixture; no comparative navigation outcome, no semantic/deep scan, and no proof for every version in the declared candidate range | "EIF has an optional Graphify adapter that behaviorally checks structural query/path/explain and validates a source-bound artifact lifecycle. Graph output remains navigation evidence; source files remain authority." | "Graphify improves navigation." "Faster codebase understanding." "Semantic analysis is verified." "All compatible versions are proven." |
| EIF can classify RTK behavioral health without treating PATH as health | OBSERVED | `scripts/tests/test_rtk_integration.py` 23/23 plus one installed-host canary (24/24) on RTK 0.43.0; `integrations/rtk/manifest.json`; generated adapter/hook contracts; `command-registry.json`; strict content-free telemetry schema | Tested adapter/version/canaries only. The tested Windows host is now `healthy` after the portable contract switched search from ambiguous `rtk grep` to native `rtk rg`; this does not prove health on every platform and no comparative token or quality result follows | "EIF has an optional RTK adapter that behaviorally reports compatible health or an explicit degraded state. On the tested Windows host, RTK 0.43.0 passes all 24 checks through the native `rtk rg` search route. Raw proxy is recorded as zero savings." | Any specific reduction percentage. "RTK improves quality." "RTK is healthy on every platform." |
| EIF improves task quality or rework rate | UNVERIFIED | No comparative study exists | No baseline, no control, no measurement methodology defined yet | "No comparative study has been run; this claim is not yet supported by evidence." | "Improves quality." "Reduces rework." "Fewer bugs." |

## How to use this table

- Writing README/website/article copy: quote the "Allowed public wording"
  column verbatim or paraphrase conservatively - never strengthen it.
- Never use anything in "Forbidden wording," even qualified ("might save
  tokens," "could improve quality") - if evidence doesn't support the
  claim, say what's actually true instead (planned, not yet measured).
- A claim only moves from `UNVERIFIED`/`NOT TESTED` to `OBSERVED` when a
  new row's Evidence column names something reproducible, added in the
  same PR that changes its status - not retroactively assumed.
