# integrations/vendor-docs/

Current, versioned library/API documentation retrieval, instead of relying
on a model's training-data knowledge. Not ported yet - see
[`integrations/README.md`](../README.md) for the degraded-mode fallback
and data-boundary notes (this integration is `external-api` by nature -
verify what a given provider actually sends before enabling it).

Planned content: provider setup guide and the "when to use this vs. trust
training data" decision guidance.
