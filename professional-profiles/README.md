# Professional profile starters

Public, installable starting points for private EIF workspace profiles.
Install one with `eifctl workspace profile install <name> --workspace-path
<path>`, then review and commit the private workspace copy.

The catalog is not a new EIF layer and installed files are not remotely
managed. The private workspace owns every installed copy; the public starter
never overwrites later customization.
